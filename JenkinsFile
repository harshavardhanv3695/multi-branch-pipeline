pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the application in main branch...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the application to production...'
            }
        }
    }
}
